// Replaced with a project-scoped public runtime key when the project is created.
// This key only grants access to this generated project's managed runtime.
export const runtimeProjectId = '';
export const runtimeAccessKey = '';
