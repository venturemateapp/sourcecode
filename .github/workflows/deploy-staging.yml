name: Deploy Staging

on:
  push:
    branches: [Staging]

concurrency:
  group: staging
  cancel-in-progress: true

env:
  APP_DIR: /data/vm/staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: Staging
    steps:
      - uses: actions/checkout@v4

      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.HOST }}
          username: ubuntu
          key: ${{ secrets.SSH_KEY }}
          envs: >
            APP_DIR,
            PORT,DB_HOST,DB_PORT,DB_USER,DB_PASSWORD,DB_NAME,DB_SSLMODE,
            SMTP_HOST,SMTP_PORT,SMTP_USER,SMTP_PASSWORD,
            S3_BUCKET,S3_REGION,AWS_ACCESS_KEY_ID,AWS_SECRET_ACCESS_KEY,
            GEMINI_API_KEY,CLAUDE_API_KEY,GROK_API_KEY,
            EMAIL_FROM_NAME,EMAIL_LOGO_URL,
            VITE_GRAPHQL_URL,VITE_AUTH_SERVER_URL,VITE_CALLBACK_URL
          script: |
            cd "$APP_DIR"

            git fetch origin
            git reset --hard origin/Staging

            cat > vm-backend/.env << ENVEOF
            PORT=$PORT
            DB_HOST=$DB_HOST
            DB_PORT=$DB_PORT
            DB_USER=$DB_USER
            DB_PASSWORD=$DB_PASSWORD
            DB_NAME=$DB_NAME
            DB_SSLMODE=$DB_SSLMODE
            SMTP_HOST=$SMTP_HOST
            SMTP_PORT=$SMTP_PORT
            SMTP_USER=$SMTP_USER
            SMTP_PASSWORD=$SMTP_PASSWORD
            S3_BUCKET=$S3_BUCKET
            S3_REGION=$S3_REGION
            AWS_ACCESS_KEY_ID=$AWS_ACCESS_KEY_ID
            AWS_SECRET_ACCESS_KEY=$AWS_SECRET_ACCESS_KEY
            GEMINI_API_KEY=$GEMINI_API_KEY
            CLAUDE_API_KEY=$CLAUDE_API_KEY
            GROK_API_KEY=$GROK_API_KEY
            EMAIL_FROM_NAME=$EMAIL_FROM_NAME
            EMAIL_LOGO_URL=$EMAIL_LOGO_URL
            ENVEOF

            cat > vm-frontend/.env << ENVEOF
            VITE_GRAPHQL_URL=$VITE_GRAPHQL_URL
            VITE_AUTH_SERVER_URL=$VITE_AUTH_SERVER_URL
            VITE_CALLBACK_URL=$VITE_CALLBACK_URL
            ENVEOF

            docker compose up --build -d
            docker system prune -af
